<UserControl x:Class="DersDagitim.Views.DataValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DersDagitim.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000" Background="{DynamicResource BackgroundBrush}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="SubHeaderStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        </Style>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="{StaticResource CardCornerRadius}"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <!-- Header -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Veri Kontrolü ve Hazırlık" Style="{StaticResource HeaderStyle}"/>
                    <TextBlock Text="Ders dağıtımı öncesi sistem kontrolü" Style="{StaticResource SubHeaderStyle}" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnAiNodeNetwork" Content="AI Düğüm Ağı" Click="BtnAiNodeNetwork_Click" 
                            Padding="20,10" Style="{StaticResource TealButton}"
                            FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="BtnAnalyze" Content="Detaylı Veri Analizi" Click="BtnAnalyze_Click" 
                            Padding="20,10" Style="{StaticResource TealButton}"
                            FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button x:Name="BtnValidate" Content="Kontrol Et" Click="BtnValidate_Click" 
                            Padding="20,10" Style="{StaticResource PrimaryButton}"
                            FontSize="14" FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Loading State -->
            <StackPanel x:Name="LoadingPanel" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,50">
                <TextBlock Text="Kontroller yapılıyor, lütfen bekleyiniz..." FontSize="16" Foreground="#86868B"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,20"/>
            </StackPanel>

            <!-- Results Panel -->
            <StackPanel x:Name="ResultsPanel" Visibility="Collapsed">

                <!-- Critical Conflicts Alert -->
                <Border x:Name="CriticalAlertBox" Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="2" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" Foreground="#DC2626" FontSize="24" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <StackPanel>
                                <TextBlock Text="KRİTİK ÇAKIŞMALAR TESPİT EDİLDİ" FontWeight="Bold" FontSize="18" Foreground="#991B1B"/>
                                <TextBlock Text="Bu sorunlar çözülmeden programın %100 dağıtılması imkansızdır." FontSize="13" Foreground="#B91C1C"/>
                            </StackPanel>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding CriticalConflicts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" BorderBrush="#FEE2E2" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,8">
                                        <DockPanel>
                                            <TextBlock Text="•" FontWeight="Bold" Foreground="#DC2626" Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="#7F1D1D" FontSize="14" FontWeight="SemiBold"/>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                
                <!-- Overall Status -->
                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Genel Durum" FontSize="14" Foreground="#86868B"/>
                            <TextBlock x:Name="TxtOverallStatus" Text="Mükemmel" FontSize="32" FontWeight="Bold" Foreground="#34C759" Margin="0,5,0,10"/>
                            
                            <WrapPanel Margin="0,15,0,0">
                                <local:StatBadge Label="Toplam Ders" Value="{Binding TotalLessons}" Color="#007AFF" Margin="0,0,10,0"/>
                                <local:StatBadge Label="Sınıf" Value="{Binding TotalClasses}" Color="#FF9500" Margin="0,0,10,0"/>
                                <local:StatBadge Label="Öğretmen" Value="{Binding TotalTeachers}" Color="#5856D6" Margin="0,0,10,0"/>
                                <local:StatBadge Label="Atanan" Value="{Binding AssignedLessons}" Color="#34C759" Margin="0,0,10,0"/>
                                <local:StatBadge Label="Atanmamış" Value="{Binding UnassignedLessons}" Color="#FF3B30"/>
                            </WrapPanel>
                        </StackPanel>
                        
                        <Grid Grid.Column="1" Width="100" Height="100">
                             <!-- Circular Progress placeholder -->
                            <Ellipse Stroke="#F2F2F7" StrokeThickness="8"/>
                            <Path x:Name="PathScoreArc" Stroke="#34C759" StrokeThickness="8" StrokeEndLineCap="Round"/>
                            <TextBlock x:Name="TxtOverallScore" Text="95%" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" FontWeight="Bold" Foreground="#1D1D1F"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Resource Heatmap -->
                <Border Style="{StaticResource CardStyle}">
                   <StackPanel>
                       <TextBlock Text="Kaynak Doluluk Haritası (Öğretmen/Sınıf)" FontSize="14" FontWeight="SemiBold" Foreground="#86868B" Margin="0,0,0,10"/>
                       <ItemsControl x:Name="HeatmapGrid" />
                       <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                           <Rectangle Width="12" Height="12" Fill="#FF3B30" RadiusX="2" RadiusY="2"/>
                           <TextBlock Text="Yetersiz" Margin="5,0,15,0" FontSize="12" Foreground="#86868B"/>
                           <Rectangle Width="12" Height="12" Fill="#FF9500" RadiusX="2" RadiusY="2"/>
                           <TextBlock Text="Sınırda" Margin="5,0,15,0" FontSize="12" Foreground="#86868B"/>
                           <Rectangle Width="12" Height="12" Fill="#34C759" RadiusX="2" RadiusY="2"/>
                           <TextBlock Text="İyi" Margin="5,0,0,0" FontSize="12" Foreground="#86868B"/>
                       </StackPanel>
                   </StackPanel>
                </Border>
                
                <TextBlock Text="Detaylı Kontroller" FontSize="20" FontWeight="SemiBold" Foreground="#1D1D1F" Margin="0,10,0,15"/>

                <!-- Check Cards -->
                <local:ValidationCard x:Name="CardAssignment" Title="Ders Atamaları" Icon="User" />
                <local:ValidationCard x:Name="CardTeacher" Title="Öğretmen Uygunluğu" Icon="Calendar" />
                <local:ValidationCard x:Name="CardResource" Title="Kaynak Dengesi" Icon="Scale" />
                <local:ValidationCard x:Name="CardSchedule" Title="Program Uygulanabilirliği" Icon="Check" />

            </StackPanel>
            
            <!-- Empty State -->
            <StackPanel x:Name="EmptyStatePanel" HorizontalAlignment="Center" Margin="0,100">
                <TextBlock Text="Sistemi kontrol etmek için butona tıklayın." FontSize="18" Foreground="#86868B"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
