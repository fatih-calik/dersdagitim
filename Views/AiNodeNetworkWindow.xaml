<Window x:Class="DersDagitim.Views.AiNodeNetworkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="AI Düğüm Ağı - Bağımlılık Analizi" Height="800" Width="1200"
        WindowStartupLocation="CenterOwner" Background="#0f172a" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#1e293b" Padding="20,15">
            <DockPanel>
                <StackPanel>
                    <TextBlock Text="AI DÜĞÜM AĞI" Foreground="White" FontSize="20" FontWeight="Bold"/>
                    <TextBlock x:Name="TxtSubTitle" Text="Öğretmenler arası bağımlılık ve sıkışıklık haritası" Foreground="#94a3b8" FontSize="12"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="#334155" CornerRadius="15" Padding="15,5" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Sistem Gerilimi: " Foreground="#94a3b8" FontSize="14"/>
                            <TextBlock x:Name="TxtStrain" Text="YÜKSEK" Foreground="#ef4444" FontSize="14" FontWeight="Bold" Margin="5,0,0,0"/>
                        </StackPanel>
                    </Border>
                    <Button Content="Yenile" Click="Refresh_Click" Padding="15,5" Style="{StaticResource CyanButton}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <wv2:WebView2 x:Name="wvMain" />
            
            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Background="#0f172a" Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="AI Analiz Ediliyor..." Foreground="White" FontSize="24" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Width="300" Height="4" Foreground="#0ea5e9" Background="#334155"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
