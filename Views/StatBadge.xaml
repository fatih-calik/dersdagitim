<UserControl x:Class="DersDagitim.Views.StatBadge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="120">
    <Border Background="{DynamicResource SurfaceAltBrush}" CornerRadius="8" Padding="12,8">
        <StackPanel>
            <TextBlock x:Name="TxtValue" Text="0" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock x:Name="TxtLabel" Text="Label" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>
