<Window x:Class="DersDagitim.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ders Dağıtım Giriş" Height="420" Width="360" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    
    <Border Background="#F8FAFC" CornerRadius="20" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.15" Color="#0F172A"/>
        </Border.Effect>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo/Branding -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,25">
                <Border Width="50" Height="50" Background="#3B82F6" CornerRadius="12" Margin="0,0,0,15">
                    <TextBlock Text="🔐" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Ders Dağıtım Giriş" FontSize="20" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
                <TextBlock Text="Lütfen şifrenizi giriniz" FontSize="13" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Input Area -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="Giriş Şifresi" FontSize="11" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,8"/>
                <Border Background="White" CornerRadius="8" BorderBrush="#CBD5E1" BorderThickness="1" Padding="4">
                    <PasswordBox x:Name="PasswordInput" FontSize="16" Height="32" VerticalContentAlignment="Center" BorderThickness="0" Background="Transparent" 
                                 Padding="5" KeyDown="PasswordInput_KeyDown"/>
                </Border>
                <TextBlock x:Name="ErrorText" Text="Hatalı şifre" Foreground="#EF4444" FontSize="11" 
                           HorizontalAlignment="Center" Margin="0,10,0,0" Visibility="Collapsed"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" Margin="0,20,0,0">
                <Button Content="Giriş Yap" Click="Login_Click" Height="38" FontSize="14" FontWeight="Bold"
                        Style="{StaticResource PrimaryButton}"/>
                
                <Button Content="Uygulamayı Kapat" Click="Close_Click" Margin="0,12,0,0" Background="Transparent" 
                        Foreground="#64748B" BorderThickness="0" FontSize="12" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
